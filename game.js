import { addGrid } from './grids.js'; //Importing the addGrid function from grids.js
addGrid();//Calling the function addGrid that generates the 100 grids

import { ships, spawn} from './ships.js';//Importing the Array that contains all ships and the spawn function that creates all ships
spawn();//By calling this imported function we spawn all ships with its lengths

